// Test generated by RoostGPT for test roost-test using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

package test

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

type Vote struct {
	CandidateID int
	Option      string
}

func TestSaveVote_Success(t *testing.T) {
	// Arrange
	candidateVotesStore := map[int]int{
		1: 0,
		2: 0,
	}
	vote := Vote{
		CandidateID: 1,
		Option:     "option1",
	}

	// Act
	err := saveVote(vote)

	// Assert
	assert.NoError(t, err)
	assert.Equal(t, candidateVotesStore[1], 2)
}

func TestSaveVote_Failure(t *testing.T) {
	// Arrange
	candidateVotesStore := map[int]int{
		1: 0,
		2: 0,
	}
	vote := Vote{
		CandidateID: 2,
		Option:     "option1",
	}

	// Act
	err := saveVote(vote)

	// Assert
	assert.Error(t, err)
	assert.Equal(t, candidateVotesStore[2], 1)
}
